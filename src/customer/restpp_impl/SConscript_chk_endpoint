# -*- mode: python; -*-
# Customers restpp endpoint checker
Import('env')

src_files = Split("""
                    restppvalidator.cpp
          """)

check_endpoints = env.Program(target='check_endpoints', source=src_files)
Return ("check_endpoints")
