#this is to demo min/max/sum/count/avg agg function in vertex attribute, grouped by vertex primary_id
create loading job n22 for graph neo4j
 load "/vagrant/data/testdata/movies.csv" to vertex movie values($0, max($0), $1, $2, count($3)) using separator=",", header="true";
 load "/vagrant/data/testdata/persons.csv" to vertex person values($0, $0, Concat($0,$1)) using separator=",", header="true";
 load "/vagrant/data/testdata/roles.csv" to edge roles values($0, $1, $2) using separator=",", header="true";
end

