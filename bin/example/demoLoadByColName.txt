drop job load_graph
create loading job load_graph for graph recommendation
  load "/vagrant/repo/product/bin/DD" 
  to vertex s_user          values($"uid"),
  to vertex s_user          values($"friendUid"),
  to vertex s_master        values(Concat("m",$"uid")),
  to vertex s_master        values(Concat("m",$"friendUid")),
  to edge   S2S_user2user values($"uid",$"friendUid","3")
  using header="true", separator=",";
  end

clear graph store

run job -n 10 load_graph

